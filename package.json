{
  "name": "promedios-grupos",
  "version": "1.0.0",
  "description": "Programa para calcular promedios de grupos de datos",
  "main": "dist/promedios.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/promedios.js",
    "serve": "live-server"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "live-server": "^1.2.2"
  }
}
